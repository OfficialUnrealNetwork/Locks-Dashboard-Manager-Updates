export declare enum AsarMode {
    NO_ASAR = 0,
    HAS_ASAR = 1
}
export declare enum EntrypointModule {
    ESM = "esm",
    CJS = "cjs"
}
export type MergeASARsOptions = {
    x64AsarPath: string;
    arm64AsarPath: string;
    outputAsarPath: string;
    singleArchFiles?: string;
};
export declare const detectAsarMode: (appPath: string) => Promise<AsarMode>;
/**
 * Determine an app entrypoint's module format from its package.json using
 * Node's own resolution rules. Anything we cannot positively identify as ESM
 * is treated as CJS, so we never ship an ESM shim on a guess.
 */
export declare const detectEntrypointModule: (packageJson: unknown) => EntrypointModule;
/**
 * Given the detected module format of each architecture's entrypoint, decide
 * which shim to ship. Refuses to build a universal app when the two
 * architectures disagree, since a single shim cannot satisfy both.
 */
export declare const resolveShimModule: (x64: EntrypointModule, arm64: EntrypointModule) => EntrypointModule;
export declare const generateAsarIntegrity: (asarPath: string) => {
    algorithm: "SHA256";
    hash: string;
};
export declare const mergeASARs: ({ x64AsarPath, arm64AsarPath, outputAsarPath, singleArchFiles, }: MergeASARsOptions) => Promise<void>;
export declare const isUniversalMachO: (fileContent: Buffer) => boolean;
